Place your ambient tree-shadow loop here.

Expected filenames:
- `tree-shadow.webm`
- `tree-shadow.mp4`

The theme will automatically try `tree-shadow.webm` first, then `tree-shadow.mp4`.
If neither file exists, the overlay stays hidden.
The ambient video is off by default and can be enabled with the hidden search command.
